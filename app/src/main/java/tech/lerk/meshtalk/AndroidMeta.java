package tech.lerk.meshtalk;

class AndroidMeta extends Meta {
    private static String TAG = AndroidMeta.class.getCanonicalName();

    static String getCoreVersion() {
        return "1.0.6"; //FIXME find way to do this dynamically m(
    }
}

